Скрипт для восстановления сорцов фронта из `.js.map`-файлов.
В скрипте переставляем путь до папки, в которой где-то могут быть `.js.map`-файлы, и путь до папки, в которую будет сохранен восстановленный проект.

 